<UserControl
    x:Class="Cs4rsa.Module.ManuallySchedule.Dialogs.Views.ShowDetailsSchoolClassesUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:Cs4rsa.WPF.Converter;assembly=Cs4rsa.WPF.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vm="clr-namespace:Cs4rsa.Module.ManuallySchedule.Dialogs.ViewModels"
    Padding="4"
    d:DataContext="{d:DesignInstance Type=vm:DetailsSchoolClassesViewModel }"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:IsNotNullConverter x:Key="IsNotNullConverter" />
        <converter:GetTeacherNameConverter x:Key="GetTeacherNameConverter" />
    </UserControl.Resources>
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Width" Value="590"></Setter>
            <Setter Property="Height" Value="245"></Setter>
            <Setter Property="MaxHeight" Value="500"></Setter>
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="CanResize"/>
            <Setter Property="ShowInTaskbar" Value="True"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Text="{Binding ClassGroupModel.Name}" />
        <TextBlock
            Grid.Row="1"
            Text="{Binding ClassGroupModel.ClassGroup.SubjectName}" />
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="{Binding ClassGroupModel.Name, StringFormat='{}{0} '}" />
            <TextBlock FontWeight="Bold" Text="{Binding ClassGroupModel.CompulsoryClass.Type.Code, StringFormat='{}{0} '}" />
            <TextBlock Text="{Binding SelectedSchoolClassModel.SchoolClassName, StringFormat='{}+ {0} '}" />
            <TextBlock FontWeight="Bold" Text="{Binding SelectedSchoolClassModel.Type.Code, StringFormat='{}{0} '}" />
        </StackPanel>
        <ListView
            Grid.Row="3"
            Margin="0,0,0,5"
            SelectionMode="Single"
            ItemsSource="{Binding SchoolClassModels}"
            SelectedItem="{Binding SelectedSchoolClassModel}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding SchoolClassName}" Header="Class name" />
                    <GridViewColumn DisplayMemberBinding="{Binding RegisterCode}" Header="Register Code" />
                    <GridViewColumn DisplayMemberBinding="{Binding Type}" Header="Type" />
                    <GridViewColumn DisplayMemberBinding="{Binding SchoolClass.EmptySeat}" Header="Remaning Seat" />
                    <GridViewColumn DisplayMemberBinding="{Binding SchoolClass.TeacherNames, Converter={StaticResource GetTeacherNameConverter}, ConverterParameter=-}" Header="Lecture" />
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Command="{Binding OpenLinkCommand}"
                Content="Open link"
            />  
            <Button
                Grid.Column="2"
                HorizontalAlignment="Right"
                Command="{Binding PickCommand}"
                Content="Select"
                IsEnabled="{Binding SelectedSchoolClassModel, Converter={StaticResource IsNotNullConverter}}"
            />
        </Grid>
    </Grid>
</UserControl>
