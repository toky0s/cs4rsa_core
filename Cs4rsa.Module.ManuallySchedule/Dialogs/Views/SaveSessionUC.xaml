<UserControl
    x:Class="Cs4rsa.Module.ManuallySchedule.Dialogs.Views.SaveSessionUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:uihelper="clr-namespace:Cs4rsa.UI.Helper;assembly=Cs4rsa.UI.Helper"
    xmlns:vm="clr-namespace:Cs4rsa.Module.ManuallySchedule.Dialogs.ViewModels;assembly=Cs4rsa.Module.ManuallySchedule"
    Width="333"
    Padding="7"
    d:DataContext="{d:DesignInstance Type=vm:SaveSessionUCViewModel,
                                     IsDesignTimeCreatable=True}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Target="{Binding ElementName=ScheduleNameTextBox}">
            Schedule _Name:
        </Label>
        <TextBox
            Name="ScheduleNameTextBox"
            Grid.Row="1"
            Margin="0,0,0,5"
            uihelper:TextBoxHelper.Placeholder="Enter schedule name..."
            KeyDown="TxtName_KeyDown"
            Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        <Button
            Grid.Row="2"
            HorizontalAlignment="Right"
            Command="{Binding SaveCommand}"
            Content="Save" />
    </Grid>
</UserControl>