<Window x:Class="cs4rsa.Views.AutoSchedule"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converter="clr-namespace:cs4rsa.Converters.DialogConverters"
        xmlns:local="clr-namespace:cs4rsa.Views"
        mc:Ignorable="d"
        Title="AutoSchedule" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <converter:Base64ImageConverter x:Key="Base64ImageConverter"/>
        <Style x:Key="InfoLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Margin="5"
              Grid.Row="0"
              Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Ellipse Height="80" 
                        Width="80"
                        Grid.Column="0">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding StudentModel.StudentInfo.Image, Converter={StaticResource Base64ImageConverter}}"
                                            Stretch="UniformToFill"/>
                </Ellipse.Fill>
            </Ellipse>
            <StackPanel Grid.Column="1"
                            Orientation="Vertical"
                            Margin="5,0,5,0">
                <TextBlock Text="{Binding StudentModel.StudentInfo.Name}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="0,17,0,0"></TextBlock>
                <TextBlock Text="{Binding StudentModel.StudentInfo.StudentId}"
                               Margin="0,5,0,0"
                           FontSize="16"></TextBlock>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1"
              Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0"
                  Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Text="Danh sách các môn của bạn"
                           Style="{StaticResource Header2}"
                           Grid.Row="0"></TextBlock>

                <ListView Grid.Row="1"
                          Style="{StaticResource Cs4rsaListView}"
                          ItemsSource="{Binding ProgramSubjectModels}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding SubjectName}"
                                           Margin="5,0,0,5"
                                           FontSize="16"></TextBlock>
                                <TextBlock Text="{Binding SubjectCode}"
                                           Margin="5,0,0,5"></TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <Image Height="15"
                                       Width="15"
                                       HorizontalAlignment="Left"
                                           Margin="5">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Source" Value="..\Images\X.png"></Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="Source" Value="..\Images\ok.png"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <TextBlock Margin="0,5,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Chưa hoàn thành"></Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="Text" Value="Đã hoàn thành"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>

                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBox Grid.Row="2"
                         Margin="0,5,0,0"
                         Height="20"></TextBox>
            </Grid>
            <Grid Grid.Column="1"
                  Margin="0,5,5,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Text="Bộ lọc"
                           Style="{StaticResource Header2}"
                           Grid.Row="0"></TextBlock>
                <StackPanel Grid.Row="1">
                    <GroupBox Header="Hiển thị">
                        <StackPanel>
                            <RadioButton Content="Các môn có thể chọn"></RadioButton>
                            <RadioButton Content="Các môn đã hoàn thành"></RadioButton>
                            <RadioButton Content="Các môn thuộc thư mục đã hoàn thành"></RadioButton>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="Sắp xếp">
                        <StackPanel>
                            <RadioButton Content="Theo tên"></RadioButton>
                            <RadioButton Content="Theo số tín chỉ"></RadioButton>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>
            <Grid Grid.Column="2"
                  Margin="0,5,5,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Text="Tất cả các cách sắp xếp"
                           Style="{StaticResource Header2}"></TextBlock>
                <ListView Grid.Row="1"
                          Style="{StaticResource Cs4rsaListView}"></ListView>
            </Grid>
        </Grid>
        <StatusBar Grid.Row="2"
                   Grid.ColumnSpan="3">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock Text="Các môn trong chương trình"></TextBlock>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <ProgressBar Width="100"
                             Height="17"
                             Maximum="100"
                             Value="{Binding ProgressValue}"></ProgressBar>
            </StatusBarItem>
            <Separator Grid.Column="2" />
        </StatusBar>
    </Grid>
</Window>
