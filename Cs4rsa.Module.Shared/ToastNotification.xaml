<Window
    x:Class="Cs4rsa.Module.Shared.ToastNotification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    AllowsTransparency="True"
    Background="Transparent"
    Focusable="False"
    Width="320"
    SizeToContent="Height"
    IsHitTestVisible="False"
    ShowActivated="False"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None">
    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.3" />
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="1"
                To="0"
                Duration="0:0:0.4" />
        </Storyboard>
    </Window.Resources>
    <Border
        MinWidth="280"
        MaxWidth="360"
        Margin="8"
        Background="#CC1E1E1E"
        CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="12"
                Opacity="0.5"
                ShadowDepth="3" />
        </Border.Effect>
        <Grid Margin="12,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!--  Icon theo loại  -->
            <TextBlock
                Foreground="White"
                x:Name="IconText"
                Grid.Column="0"
                Margin="0,0,10,0"
                VerticalAlignment="Top"
                FontSize="18"
                Text="ℹ️" />
            <StackPanel Grid.Column="1">
                <TextBlock
                    x:Name="TitleText"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="Title"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="MessageText"
                    Margin="0,3,0,0"
                    FontSize="12"
                    Foreground="#CCCCCC"
                    Text="Message"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Border>
</Window>